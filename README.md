To start you need Lumerical package license

SETUP:

python -m venv venv

add lumapi.pth file into venv/lib/site-packages (change path in it if neccessary)

venv/scripts/activate

pip install -r requirements.txt


